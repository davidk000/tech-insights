# Triton编程 第3节

> 时间档: 18:00 | 日期: 2026-04-03 | Track: track_c

🌙 晚档 | Triton编程 第3/7节
━━━━━━━━━━━━━━━━━━

## 📌 今日课题
**Triton实战-Softmax Kernel：行级/块级/自动调优/性能对比CUDA**

---

## 📖 完整讲解

## 实战：写一个比 NumPy 快 10 倍的 Softmax

### 数值稳定 Softmax 实现

```python
@triton.jit
def softmax_kernel(output_ptr, input_ptr, n_rows, n_cols, BLOCK_SIZE: tl.constexpr):
    row_idx = tl.program_id(0)
    row_start = row_idx * n_cols
    cols = tl.arange(0, BLOCK_SIZE)
    mask = cols < n_cols
    x = tl.load(input_ptr + row_start + cols, mask=mask, other=0.0)
    x_max = tl.max(x, axis=0)
    x_norm = x - x_max
    numerator = tl.exp(x_norm)
    denominator = tl.sum(numerator, axis=0)
    y = numerator / denominator
    tl.store(output_ptr + row_start + cols, y, mask=mask)
```

### 性能关键

1. 数值稳定 Softmax：`x - max(x)` 防止 exp() 上溢
2. 自动调优（autotune）：自动试 16/32/64/128 的 BLOCK_SIZE
3. 算子融合：把 exp + sum + divide 合并成一个 kernel，减少 HBM 读写

### 效果对比

| 实现 | 速度 |
|:---|:---:|
| NumPy | 1× |
| PyTorch CUDA | 15× |
| Triton | 18× |

---

🏷️ 课程: Triton编程
📅 周五 2026-04-03 18:00
⏱️ 第3节/共7节


---
*归档时间: 2026-04-03 18:00:06*
