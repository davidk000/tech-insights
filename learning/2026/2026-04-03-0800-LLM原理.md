# LLM原理 第3节

> 时间档: 08:00 | 日期: 2026-04-03 | Track: track_a

🌅 早档 | LLM原理 第3/14节
━━━━━━━━━━━━━━━━━━

## 📌 今日课题
**Multi-Head Attention：多头并行/不同注意力头捕获不同关系/残差连接+LayerNorm**

---

## 📖 完整讲解

## 原理

多个注意力头并行工作，每个头学习不同的注意力模式。

一个头的痛点：参数有限，表达能力受限。多头解法：将 Q/K/V 分成 h 份，每份独立做 Attention，最后 concat 回来。

每个头「擅长」捕获不同类型的关系：
- 头1：句法结构（主谓宾）
- 头2：指代关系（他/她/它）
- 头3：语义相似
- 头4：位置邻近关系

残差连接 + LayerNorm 保障梯度流动，深度再深也不怕。

## 代码示例

```python
class MultiHeadAttention(nn.Module):
    def __init__(self, d_model, num_heads):
        super().__init__()
        self.num_heads = num_heads
        self.d_k = d_model // num_heads
        self.W_q = nn.Linear(d_model, d_model)
        self.W_k = nn.Linear(d_model, d_model)
        self.W_v = nn.Linear(d_model, d_model)
        self.W_o = nn.Linear(d_model, d_model)

    def forward(self, Q, K, V, mask=None):
        B = Q.size(0)
        Q = self.W_q(Q).view(B, -1, self.num_heads, self.d_k).transpose(1,2)
        K = self.W_k(K).view(B, -1, self.num_heads, self.d_k).transpose(1,2)
        V = self.W_v(V).view(B, -1, self.num_heads, self.d_k).transpose(1,2)
        out, _ = self_attention(Q, K, V, mask)
        out = out.transpose(1,2).contiguous().view(B, -1, self.num_heads*self.d_k)
        return self.W_o(out)
```

## 今日思考题

1. 如果 head 数从 8 增到 16，每个 head 的 d_k 怎么变？
2. 为什么有些头在训练后可以被剪掉而不影响效果？

---

🏷️ 课程: LLM原理
📅 周五 2026-04-03 08:00
⏱️ 第3节/共14节


---
*归档时间: 2026-04-03 08:02:37*
